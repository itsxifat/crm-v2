// Agency detail view — same screen as the freelancer detail page, but served under
// /admin/agencies so it is gated by hr.agencies.manage (see ROUTE_PERMISSIONS).
export { default } from '../../freelancers/[id]/page'
